import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    org.AFumigatus293.eg,
    org.AFumigatus293.eg_dbconn,
    org.AFumigatus293.eg_dbfile,
    org.AFumigatus293.eg_dbschema,
    org.AFumigatus293.eg_dbInfo,

    org.AFumigatus293.egORGANISM
)

